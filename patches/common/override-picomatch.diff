Override picomatch to ^4.0.4 (top-level) and ^2.3.2 (nested for 2.x consumers).
Uses nested overrides because the dependency tree spans major versions 2.x and 4.x.

@generated
@generator: scripts/apply-override.sh --patch common/override-picomatch.diff --override 'global:picomatch=^4.0.4' --override 'nested:anymatch:picomatch=^2.3.2' --override 'nested:micromatch:picomatch=^2.3.2' --override 'nested:readdirp:picomatch=^2.3.2' --override 'remote/package.json@global:picomatch=^4.0.4'
@override-package: picomatch@^4.0.4
@override-package: picomatch@^2.3.2

Index: b/package.json
===================================================================
--- a/package.json
+++ b/package.json
@@ -242,7 +242,17 @@
     "postcss": "^8.4.31",
     "@tootallnate/once": "^3.0.1",
     "node-gyp-build": "4.8.1",
-    "serialize-javascript": "^7.0.3"
+    "serialize-javascript": "^7.0.3",
+    "picomatch": "^4.0.4",
+    "anymatch": {
+      "picomatch": "^2.3.2"
+    },
+    "micromatch": {
+      "picomatch": "^2.3.2"
+    },
+    "readdirp": {
+      "picomatch": "^2.3.2"
+    }
   },
   "repository": {
     "type": "git",
Index: b/remote/package.json
===================================================================
--- a/remote/package.json
+++ b/remote/package.json
@@ -44,6 +44,7 @@
   },
   "overrides": {
     "undici": "^7.24.0",
-    "node-gyp-build": "4.8.1"
+    "node-gyp-build": "4.8.1",
+    "picomatch": "^4.0.4"
   }
 }
